import { DropdownfilterPipe } from './dropdownfilter.pipe';

describe('DropdownfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
